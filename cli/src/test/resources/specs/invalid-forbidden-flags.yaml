metadata:
  name: bad-flags
jvm:
  args:
    - "-Xms256m"
    - "-Xlog:gc*:file=gc.log"
benchmarks:
  - id: ephemeral-g1
    source:
      type: internal
      module: benchmark-ephemeral-jmh
    jvm:
      args:
        - "-XX:StartFlightRecording=filename=test.jfr"
